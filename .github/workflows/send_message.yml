name: telegram message
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: send telegram message on push
        uses: katearonova/tz3@main
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.AAE7PPRAB0v7FHrMjOmLjxEnbOcmUidEe0I }}
          message: |
            Hi
